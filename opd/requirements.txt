# opd — the project's ONE venv, trainer half (collector half:
# collector-requirements.txt beside this file; the in-process teacher
# scorer needs only the library + numpy, both already here). The library
# arrives by RELEASE URL (the published install-by-URL contract). When an
# index exists, this line becomes:
#   gsj-envloader==0.8.0
gsj-envloader @ https://github.com/MHGanainy/gsj-envloader/releases/download/v0.8.0/gsj_envloader-0.8.0-py3-none-any.whl

# Trainer stack. torch: 2.13.0 from the PyTorch cu129 index — the SAME
# version the frozen collector set pins, in the CUDA build that runs on
# this H200's 12.8 driver (minor-version compatibility; PyPI's default
# 2.13.0 wheel is a cu130 build that does not). One venv runs both
# halves — match the index to your own driver.
--extra-index-url https://download.pytorch.org/whl/cu129
torch==2.13.0
transformers==5.14.1
peft==0.20.0
huggingface_hub==1.26.0
